import { AppContainer } from "../AppContainer/AppContainer";
import styles from "./Footer.module.css";

export const Footer = () => {
  return (
    <footer className={styles.footer}>
      <AppContainer>Copyright 2026</AppContainer>
    </footer>
  );
};
